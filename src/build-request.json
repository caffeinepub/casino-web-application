{
  "kind": "build_request",
  "title": "Set uploaded image as default site logo fallback asset",
  "priority": "normal",
  "requirements": [
    {
      "id": "REQ-12",
      "text": "Create a default site logo fallback image from the user-provided upload and add it as a static asset at `frontend/public/assets/generated/site-logo-fallback.dim_256x256.png`, formatted to a 256×256 square and preserving transparency if present.",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-1"
        ],
        "quotes": [
          "Here is the site logo"
        ]
      },
      "acceptanceCriteria": [
        "`frontend/public/assets/generated/site-logo-fallback.dim_256x256.png` exists and is 256×256.",
        "When no custom `site-logo` asset is stored in the backend, the header logo renders using the new fallback image.",
        "If the header logo image fails to load, the UI still falls back to `/assets/generated/site-logo-fallback.dim_256x256.png` without breaking layout."
      ]
    },
    {
      "id": "REQ-13",
      "text": "Ensure the generated fallback logo asset is served directly from the frontend static assets (do not route it through the backend canister).",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-1"
        ],
        "quotes": [
          "Here is the site logo"
        ]
      },
      "acceptanceCriteria": [
        "The app requests the logo fallback via the path `/assets/generated/site-logo-fallback.dim_256x256.png`.",
        "No backend endpoints or blob storage are required for the fallback logo to display."
      ]
    }
  ],
  "constraints": [
    "Use English for any user-facing text.",
    "Do not edit files under `frontend/src/components/ui`.",
    "Keep the backend as a single Motoko actor; no new backend services."
  ],
  "nonGoals": [
    "Changing the admin-uploaded `site-logo` asset behavior (this request only provides the default fallback logo).",
    "Creating or updating a favicon/app icon set."
  ],
  "imageRequirements": {
    "required": [
      "Gold coin mascot logo on transparent background, centered with padding (site-logo-fallback.dim_256x256.png)"
    ],
    "edits": []
  }
}